# Harness: regenerable review diff packages, and the per-contributor settings file below.
# Everything else the harness writes is tracked on purpose — a ledger a second person cannot
# read is not a ledger.
*.diff

# Per-contributor: Claude Code writes local permission grants here. Never shared, never tracked.
.claude/settings.local.json
